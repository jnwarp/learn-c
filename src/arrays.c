#include <stdio.h>

int main() {
	int numbers[10];

	numbers[0] = 10;
	numbers[1] = 20;
	numbers[2] = 30;
	numbers[3] = 40;

	printf("The fourth number in the array is %d", numbers[3]);

	return 0;
}
