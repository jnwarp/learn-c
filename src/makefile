all: main.c variables.c arrays.c
	#----------------------BUILD----------------------
	gcc -g -Wall -o main main.c
	gcc -g -Wall -o variables variables.c
	gcc -g -Wall -o arrays arrays.c
	#---------------------- FIN ----------------------

clean:
	$(RM) main
	$(RM) variables
	$(RM) arrays
